# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# Packed builds
*.crx
*.pem
*.zip

# Nothing here needs node_modules — the tests stub the chrome.* APIs — but
# don't let a stray install get committed.
node_modules/
